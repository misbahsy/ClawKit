{
  "name": "telegram",
  "category": "channels",
  "description": "Telegram channel via grammY. Polling mode. Text, photos, docs, voice, video, stickers. Groups.",
  "npmDependencies": {
    "grammy": "^1.30.0"
  },
  "suggests": ["queue-per-group"],
  "suggestReason": {
    "queue-per-group": "Per-group queue handles concurrent group chat messages gracefully."
  },
  "configSchema": {
    "botToken": { "type": "string", "default": "" }
  },
  "importName": "createTelegramChannel",
  "instanceTemplate": "createTelegramChannel(config.channels?.telegram ?? {})",
  "phase": 3
}
