{
  "name": "whatsapp",
  "category": "channels",
  "description": "WhatsApp channel via Baileys. QR auth. Text, images, voice, docs. Groups. Reconnection with backoff.",
  "npmDependencies": {
    "baileys": "^6.7.16",
    "@hapi/boom": "^10.0.1",
    "pino": "^9.6.0",
    "qrcode-terminal": "^0.12.0"
  },
  "suggests": ["prompt-workspace", "queue-per-group"],
  "suggestReason": {
    "prompt-workspace": "Workspace prompt reads identity files for richer WhatsApp responses.",
    "queue-per-group": "Per-group queue handles concurrent group chat messages gracefully."
  },
  "configSchema": {
    "authDir": { "type": "string", "default": "./data/whatsapp-auth" }
  },
  "importName": "createWhatsAppChannel",
  "instanceTemplate": "createWhatsAppChannel(config.channels?.whatsapp ?? {})",
  "phase": 2
}
