{
  "name": "clawkit-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace=packages/cli",
    "test": "vitest run",
    "test:smoke": "vitest run --config vitest.smoke.config.ts",
    "test:agents": "vitest run --config vitest.smoke.config.ts tests/smoke/agents.smoke.test.ts",
    "test:channels": "vitest run --config vitest.smoke.config.ts tests/smoke/channels.connectivity.test.ts",
    "test:all": "vitest run && vitest run --config vitest.smoke.config.ts",
    "dev": "npm run dev --workspace=packages/cli"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/ws": "^8.18.1",
    "better-sqlite3": "^12.6.2",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "ws": "^8.19.0"
  }
}
